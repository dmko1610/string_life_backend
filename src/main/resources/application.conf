ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [dmitrykovalev.stringlife.ApplicationKt.module]
    }
}

database {
    url = ${DATABASE_URL}
    user = ${DATABASE_USER}
    password = ${DATABASE_PASSWORD}
}
